name: db-tests

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Run migrations and SQL tests on local PostgreSQL
        run: ./scripts/test-db.sh
